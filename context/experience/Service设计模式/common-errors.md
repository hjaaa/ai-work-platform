## 2026-04-09 文件操作与 DB 操作的顺序

- **现象**：Service 方法中先写 DB、再操作文件，文件失败时 DB 已提交，导致数据库与磁盘状态不一致
- **原因**：没有考虑到文件操作不参与数据库事务，必须手动保证执行顺序
- **正确做法**：文件操作先于 DB 操作执行。文件失败直接抛 BusinessException，不进入 DB 操作，事务自然回滚
- **适用场景**：任何 Service 方法中同时涉及文件 I/O 和数据库写入（如 Skill、文档、配置文件同步等）
