## 代码实施完成后自检清单

- [ ] ProcessBuilder/外部进程调用逻辑是否有重复？是否应该复用 `ProcessExecutor` 工具类？
- [ ] Controller 是否直接注入了 Mapper？必须通过 Service 层访问数据
- [ ] 是否有定义了但未使用的类/方法（死代码）？实施结束后全局搜索确认
- [ ] 文件树/递归遍历类操作是否有深度和节点数限制？防止大项目阻塞
- [ ] 同一请求中是否有重复的 DB 查询？查询结果应传参复用
