# 前端开发规范（团队规范）

适用范围：`ai-work-ui`（Vue 3 + TypeScript + Vite）。

> 🚧 规范文档待补充。引入正式规范前，暂按以下基线执行：
>
> - 组件与代码风格遵循 [Vue 官方风格指南](https://vuejs.org/style-guide/)（Priority A/B 必须遵守）
> - TypeScript 开启并保持 strict 模式，不使用 `any` 规避类型检查
> - 遵循 `ai-work-ui` 项目内现有的 ESLint / Prettier 配置，提交前保证 lint 通过
