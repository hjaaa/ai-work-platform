== Current State ==
status: completed
phase: done
last_updated: 2026-04-07

== What Worked ==
- 本地 Maven 构建 + Docker 运行时打包（避免 Docker 内下载依赖超时）
- deploy.sh 自动检测 JDK 21
- host.docker.internal 解决容器访问宿主机 MySQL/Redis
- 重命名 logback-test.xml → logback-profile-test.xml 避免 Logback 保留名冲突

== What Did NOT Work ==
- Docker 内多阶段 Maven 构建 → 失败，Maven 下载依赖太慢导致 Docker RPC 断开（不要再试）
- npm ci → 失败，本地与 Docker 内 npm 版本不同导致 lock 文件不兼容，改用 npm install
- logback-test.xml 文件名 → Logback 自动发现此文件作为主配置，但内容是 <included> 格式导致启动失败

== Exact Next Step ==
已完成

== Files State ==
- Dockerfile [created] - 运行时打包（本地 Maven 预构建）
- .dockerignore [created]
- platform-frontend/Dockerfile [created] - Node build → Nginx
- platform-frontend/.dockerignore [created]
- platform-frontend/nginx/default.conf [created]
- docker-compose.yml [created]
- .env.example [created]
- .env [created, gitignored]
- deploy.sh [created] - 自动 JDK 检测 + 一键部署
- logback-spring.xml [modified] - include 路径更新
- logback-dev.xml → logback-profile-dev.xml [renamed]
- logback-test.xml → logback-profile-test.xml [renamed]
- logback-prod.xml → logback-profile-prod.xml [renamed]
